# The following patch sets conversion strategy for the CRD.
# Only one API version (v1alpha1) exists, so no conversion is needed.
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: etcdbackupschedules.etcd.aenix.io
spec:
  conversion:
    strategy: None
