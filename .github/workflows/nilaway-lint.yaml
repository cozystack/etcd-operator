name: Nilaway lint

on:
  pull_request:

jobs:
  nilaway-lint:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4.1.7
      - uses: actions/setup-go@v6.4.0
        with:
          go-version: 1.23.0
      - run: make nilaway-lint
